{
  "version": 2,
  "builds": [
    {
      "src": "server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/routes/$1"
    }
  ],
  "env": {
    "NODE_ENV": "production"
  },
  "build": {
    "command": "npm install && npm start"
  }
}